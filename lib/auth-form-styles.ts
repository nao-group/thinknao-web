import { rem } from "@mantine/core";
import { INK } from "@/constants/colors";

export const AUTH_INPUT_STYLES = {
  input: {
    borderRadius: rem(10),
    fontSize: rem(14),
  },
  label: {
    fontWeight: 500,
    fontSize: rem(14),
    color: INK,
    marginBottom: rem(6),
  },
};
